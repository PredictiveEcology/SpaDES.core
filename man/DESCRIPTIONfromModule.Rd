% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DESCRIPTIONfromModule.R
\name{DESCRIPTIONfromModule}
\alias{DESCRIPTIONfromModule}
\title{Make \code{DESCRIPTION} file(s) from SpaDES module metadata}
\usage{
DESCRIPTIONfromModule(
  modules,
  modulePath,
  projectPath = ".",
  singleDESCRIPTION = FALSE,
  package,
  title,
  date,
  description,
  version,
  authors,
  suggests = c("knitr", "rmarkdown", "testthat", "withr", "roxygen2"),
  merge = TRUE,
  write = TRUE,
  verbose = getOption("Require.verbose"),
  metadataList,
  ...
)
}
\arguments{
\item{modules}{A character vector of module names.}

\item{modulePath}{Character. The path containing the modules, usually
\code{modulePath()} or \code{paths$modulePath}.}

\item{projectPath}{Character. Only used when \code{singleDESCRIPTION = TRUE}.}

\item{singleDESCRIPTION}{Logical. If \code{TRUE}, one \code{DESCRIPTION} is written for
all \code{modules} combined, with redundant \code{reqdPkgs} entries trimmed across
them. If \code{FALSE} (default), one per module, written beside the module.}

\item{package, title, date, description, version, authors}{Optional overrides for
the corresponding \code{DESCRIPTION} fields. Each defaults to the value in the
module metadata.}

\item{suggests}{Character vector written to \code{Suggests}.}

\item{merge}{Logical. If \code{TRUE} (default) and a \code{DESCRIPTION} already exists
at the destination, its \code{Imports}/\code{Suggests}/\code{Remotes} are merged with the
generated ones rather than replaced, so entries added by hand survive
regeneration.}

\item{write}{Logical. If \code{TRUE} (default), write the file(s); if \code{FALSE},
write to a temporary file instead, which is useful for inspection and in CI.}

\item{verbose}{Numeric or logical. Passed to \code{Require}'s verbosity.}

\item{metadataList}{Optional pre-parsed module source, as from
\verb{parse(<module>.R, keep.source = TRUE)}. Supply it to avoid re-parsing.}

\item{...}{Currently unused.}
}
\value{
Invisibly, the path(s) of the \code{DESCRIPTION} file(s) written.
}
\description{
A SpaDES module declares its dependencies in \code{reqdPkgs} inside \code{defineModule()},
not in a \code{DESCRIPTION}. This translates that metadata into a \code{DESCRIPTION}:
it is what \code{\link[=convertToPackage]{convertToPackage()}} needs in order to turn a module into an R
package, and what \code{SpaDES.project::makeDESCRIPTION()} uses to give a whole
project one dependency manifest that \code{pak} or \code{renv} can act on.

Note that module CI does \emph{not} need this: the shared \code{render-module-rmd}
workflow resolves a module's dependencies with \code{\link[=packages]{packages()}} and
\code{Require::Require()} directly from the metadata.

\code{Imports} comes from \code{reqdPkgs}, with version inequalities preserved and
\code{SpaDES.core} added if absent. Any \code{reqdPkgs} entry naming a GitHub
repository also becomes a \code{Remotes} entry, so the resulting \code{DESCRIPTION} is
installable by \code{pak} without further help. A \code{(HEAD)} specification is
resolved to \verb{>= <version currently on the repository>}, since \code{HEAD} is not
something a \code{DESCRIPTION} can express.
}
