% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation-spades.R
\name{saveSimOnExitSimInit}
\alias{saveSimOnExitSimInit}
\title{Save the \code{simList} on exit from \code{simInit}, mirroring \code{saveSimOnExit()}}
\usage{
saveSimOnExitSimInit(recoverMode, sim, rmo)
}
\description{
Like its \code{spades} counterpart, this is registered via \code{on.exit} inside
\code{simInit} so it fires whether \code{simInit} completes cleanly or is interrupted
(e.g., an error in a module's \code{.inputObjects}). On a non-clean exit, and when
\code{options('spades.recoveryMode') > 0}, the accumulated recovery object (\code{rmo})
is written into the \code{simList} so that \code{\link[=restartSimInit]{restartSimInit()}} can rewind the state
to the start of the interrupted \code{.inputObjects}. The clean/interrupted
distinction uses a separate flag (\code{.pkgEnv$.cleanEndSimInit}) so it does not
interfere with the \code{spades} event-recovery flag (\code{.pkgEnv$.cleanEnd}).
}
\keyword{internal}
