% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eventStops.R
\name{.parseEventStops}
\alias{.parseEventStops}
\alias{.matchesEventSpec}
\title{Parse \code{spades(events = )} once into a barrier object and a whitelist}
\usage{
.parseEventStops(events)

.matchesEventSpec(event, spec)
}
\arguments{
\item{events}{The \code{events} argument of \code{\link[=spades]{spades()}}.}

\item{event}{One element of the event queue, i.e. a list with \code{moduleName} and
\code{eventType}.}

\item{spec}{A named list of event types per module, as \code{events} itself takes, or
a bare character vector meaning those event types in any module.}
}
\value{
A list of two elements: \code{eventsBeforeAfter}, either \code{NULL} (no barrier
given, so nothing to test per event) or a list with non-\code{NULL} \code{before} and/or
\code{after} specifications; and \code{events}, the whitelist with the reserved entries
removed, or \code{NULL} if nothing else was named.

logical(1). \code{FALSE} for an empty or \code{NULL} \code{spec}, so a specification
that names nothing stops nothing.
}
\description{
Parse \code{spades(events = )} once into a barrier object and a whitelist

Does an event match an event specification?
}
\keyword{internal}
