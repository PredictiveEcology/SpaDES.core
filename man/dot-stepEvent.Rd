% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation-spades.R
\name{.stepEvent}
\alias{.stepEvent}
\title{Process one event with recovery wrapping}
\usage{
.stepEvent(
  sim,
  recoverMode,
  allObjNames,
  thisSpadesCallRandomStr,
  debug,
  notOlderThan,
  events = NULL,
  ...
)
}
\arguments{
\item{sim}{A \code{simList}.}

\item{recoverMode}{Numeric/logical; \verb{> 0} enables the pre/post snapshots.}

\item{allObjNames}{Per-module object-name list to snapshot (output objects for the
\code{spades} loop; input objects for the \code{.inputObjects} phase). Passed to
\code{recoverModePre()}.}

\item{thisSpadesCallRandomStr}{Scratch-dir token for file-backed snapshots.}

\item{debug, notOlderThan, events, ...}{Passed through to \code{doEvent()}.}
}
\value{
The updated \code{simList} (with \code{sim@.xData[["._rmo"]]} advanced).
}
\description{
The single-event step shared by the \code{spades} event loop and (later) the \code{simInit}
\code{.inputObjects} phase: take a recovery snapshot (\code{recoverModePre}), run the next event
via \code{doEvent()}, then record any events it added (\code{recoverModePost}). Both recovery
calls are skipped when \code{recoverMode <= 0}.
}
\details{
The recovery accumulator is kept on \code{sim@.xData[["._rmo"]]} (an environment slot)
rather than threaded as a return value. This is deliberate: \code{recoverModePre} snapshots
the state \emph{before} the event, and if \code{doEvent()} then errors, this function never
returns -- so a returned accumulator would be lost exactly when recovery is needed.
Mutating the environment slot makes the pre-event snapshot visible to the caller's
\code{on.exit} handler regardless of whether the event succeeds.
}
\keyword{internal}
