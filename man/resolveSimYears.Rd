% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simList-accessors.R
\name{resolveSimYears}
\alias{resolveSimYears}
\title{Resolve a module's simulation years against the simulation clock}
\usage{
resolveSimYears(years, sim)
}
\arguments{
\item{years}{numeric of length 2, typically \code{P(sim)$simYears}. If every
element is \code{NA}, the simulation's own start and end times are used instead.}

\item{sim}{A \code{simList} object.}
}
\value{
A numeric of length 2, giving the start and end years to use.
}
\description{
Summary modules typically take a \code{simYears} parameter giving the two years to
compare. Its default is \code{c(NA, NA)}, meaning "use whatever the simulation
itself is running", so that the parameter cannot silently disagree with the
\code{simList}. This resolves the one against the other, and exists so that every
module performs that resolution identically.
}
\seealso{
\code{\link[=startEnd]{startEnd()}}
}
\author{
Eliot McIntire
}
