% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moduleRmdToVignette.R
\name{moduleRmdToVignette}
\alias{moduleRmdToVignette}
\title{Add a module's documentation \code{.Rmd} to its package rendition as a vignette}
\usage{
moduleRmdToVignette(module, pkgPath)
}
\arguments{
\item{module}{Character string of module name, without path.}

\item{pkgPath}{Character string. The package rendition of the module, as
returned by \code{convertToPackage(module, destinationPath = )}.}
}
\value{
Invisibly, the path of the vignette written,
\code{file.path(pkgPath, "vignettes", paste0(module, ".Rmd"))}.
}
\description{
A module documents itself in \verb{<module>.Rmd}, written from the \code{\link[=newModule]{newModule()}}
template. To publish it as an article of a \pkg{pkgdown} site built from the
package rendition that \code{\link[=convertToPackage]{convertToPackage()}} makes, it has to move into
\verb{vignettes/}, and three things in the template assume it does not:
}
\details{
\itemize{
\item \strong{the module path.} The template's R code -- the \code{subtitle} and
authors line, and the \code{moduleInputs()}, \code{moduleParams()} and
\code{moduleOutputs()} tables -- passes the module path as \code{".."}, relative
to the \code{.Rmd}. From \verb{vignettes/} that is \code{"../.."}. Only R code (code
chunks and inline \code{`r `} expressions) is rewritten: the template's
prose also shows \code{".."}, as advice to the reader, and is left alone;
\item \strong{the output format.} The template renders with
\code{bookdown::html_document2}, whose numbered table cross-references
(\verb{\\@ref(tab:...)}) \pkg{pkgdown} would otherwise replace with its own
format and leave unresolved. \code{pkgdown: as_is: true} is added to the YAML
header, unless the header already has a \code{pkgdown} entry;
\item \strong{the \verb{citations/} and \verb{figures/} directories}, which the header's
\code{bibliography} and the text refer to by relative path. They are copied
into \verb{vignettes/}.
}

\verb{\pkg{pkgdown}} builds from an \emph{installed} package, so install the package
rendition before \code{pkgdown::build_site(install = FALSE)}.
}
\examples{
\dontrun{
pkg <- convertToPackage("myModule", path = "modules", destinationPath = tempfile())
moduleRmdToVignette("myModule", pkg)
install.packages(pkg, repos = NULL, type = "source")
pkgdown::build_site(pkg, install = FALSE)
}
}
\seealso{
\code{\link[=convertToPackage]{convertToPackage()}}
}
