% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restart.R
\name{.restartModuleToReparse}
\alias{.restartModuleToReparse}
\title{Identify the module whose source should be re-parsed on restart}
\usage{
.restartModuleToReparse(
  sim,
  recoverNames = names(sim$.recoverableObjs),
  eventType = NULL
)
}
\arguments{
\item{sim}{A \code{simList}.}

\item{recoverNames}{Character names of \code{sim$.recoverableObjs} (most-recent first).}

\item{eventType}{Optional event type to which the queued-event fallback is restricted.}
}
\value{
A single module name, or \code{NULL} if none can be identified.
}
\description{
Picks the module to re-\code{parse()}, most-reliable source first: (1) the most-recent
recovery snapshot in \code{sim$.recoverableObjs} (the interrupted module), (2) \code{sim@current}
(set while a module event is running), then (3) the first still-queued event's module
(optionally restricted to \code{eventType}, e.g. \code{".inputObjects"}) -- mirroring
\code{\link[=restartSpades]{restartSpades()}}'s use of \code{events(sim)}. (3) is the fallback when \code{.recoverableObjs}
is empty, e.g. a cached earlier \code{.inputObjects} swapped \code{sim@.xData} and orphaned the
recovery accumulator.
}
\keyword{internal}
