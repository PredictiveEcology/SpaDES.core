% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{suggestModules}
\alias{suggestModules}
\title{Check that a module's declared inputs are present, and suggest modules}
\usage{
suggestModules(sim, suggestedModules = NULL, module = currentModule(sim))
}
\arguments{
\item{sim}{A \code{\link[=simList]{simList()}} object.}

\item{suggestedModules}{Optional character vector of module names to name in
the error message as the likely providers of the missing objects.}

\item{module}{Character string naming the module whose \code{inputObjects} to
check. Defaults to the currently running module.}
}
\value{
Invisibly, a character vector of the missing object names -- which is
\code{character(0)} unless the function stops first.
}
\description{
Call this inside an event (typically \code{init}) to fail early, and with a
useful message, when the objects a module declares in its metadata
\code{inputObjects} have not been supplied. Without it, the module runs on and
fails later at whichever line first touches a missing object, which is
usually a long way from the real cause: a module that should have run first
was not included in the \code{simInit()} call.
}
\examples{
sim <- simInit()
## nothing is declared, so nothing is missing
suggestModules(sim)

}
\seealso{
\code{\link[=checkObject]{checkObject()}}, \code{\link[=suppliedElsewhere]{suppliedElsewhere()}}
}
\author{
Eliot McIntire and Alex Chubaty
}
