% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restart.R
\name{.restartRestoreEventObjs}
\alias{.restartRestoreEventObjs}
\title{Restore one recovered event's object state on restart (shared rewind core)}
\usage{
.restartRestoreEventObjs(sim, snapshot, modSnapshot, module, label = module)
}
\arguments{
\item{sim}{A \code{simList}.}

\item{snapshot}{Named list of the event's objects at its start (\code{sim$.recoverableObjs[[i]]}).}

\item{modSnapshot}{Named list of the module's \code{mod} objects at the event's start, or \code{NULL}.}

\item{module}{The module name whose objects are being rewound.}

\item{label}{Text used in the "Setting all changed objects ..." message (defaults to \code{module}).}
}
\value{
\code{sim}, invisibly.
}
\description{
Shared by \code{\link[=restartSpades]{restartSpades()}} and \code{\link[=restartSimInit]{restartSimInit()}}. For a single recovered event,
rewinds the \code{simList}'s objects to their pre-event state: (a) any object the event
\emph{creates} (its \code{outputObjects}) that is absent from the snapshot did not exist when the
event began, so it is removed; (b) the snapshotted objects are copied back into
\code{sim@.xData}; (c) the module's \code{mod} objects are restored. Mutates \code{sim@.xData} by side
effect. The two restart paths previously each had their own (slightly divergent) copy of
this; sharing it means a fix in one benefits both.
}
\keyword{internal}
