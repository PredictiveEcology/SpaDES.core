% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paths.R
\name{findProjectPath}
\alias{findProjectPath}
\title{Find the project root directory}
\usage{
findProjectPath(path = getwd())
}
\arguments{
\item{path}{The directory to search upward from. Defaults to \code{\link[=getwd]{getwd()}}.}
}
\value{
An absolute path to the project root, or \code{path} if no project marker
is found above it.
}
\description{
Searches upward from \code{path} for an RStudio project file (a \code{.Rproj} whose
first line is \verb{Version: }) or a git repository (a \code{.git} directory, or a
\code{.git} file pointing elsewhere, as used by worktrees and submodules),
falling back on \code{path} itself when neither is found.
}
\details{
This uses \pkg{rprojroot} when it is available. \pkg{rprojroot} is in
\code{Suggests}, so when it is not installed this simply returns \code{path}, which is
the same answer the fallback gives.
}
\examples{
findProjectPath()                 # the project the session is working in
findProjectPath(tempdir())        # no markers above tempdir(); returns it
}
