% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation-simInit.R
\name{.runInputObjectsPhase}
\alias{.runInputObjectsPhase}
\title{Run (or resume) a \code{simList}'s \code{.inputObjects} phase}
\usage{
.runInputObjectsPhase(
  sim,
  recoverMode,
  allInputObjNames,
  thisSpadesCallRandomStr,
  modulesLoaded
)
}
\arguments{
\item{sim}{A \code{simList} whose \code{sim@.xData[["._simInitContext"]]} has been populated.}

\item{recoverMode, allInputObjNames, thisSpadesCallRandomStr}{Recovery parameters passed
through to \code{\link[=.stepEvent]{.stepEvent()}}. \code{simInit} passes its values; \code{restartSimInit} passes
\code{recoverMode = 0} (a resume does not re-arm recovery).}

\item{modulesLoaded}{The loaded-modules list with the core modules already appended;
the user modules are appended here.}
}
\value{
A finished \code{simList}.
}
\description{
The continuation shared by \code{\link[=simInit]{simInit()}} and \code{\link[=restartSimInit]{restartSimInit()}}: schedule each module's
\code{.inputObjects} as a real event, drain that phase through the same single-event step as
the \code{spades} loop (\code{\link[=.stepEvent]{.stepEvent()}}), then schedule init events, fill dot-params, and
finish initialization (\code{\link[=.finishSimInit]{.finishSimInit()}}). Having one implementation means the normal
and resumed paths cannot drift.
}
\details{
It is re-entrant: modules whose \code{.inputObjects} already completed (recorded in
\code{completed(sim)}) are not re-scheduled, and init events that already exist are not
duplicated. So \code{restartSimInit()} simply rewinds, removes the completed-marker of the
module(s) to re-run, and calls this -- mirroring how \code{restartSpades()} hands back to
\code{spades()}.

The drain loop is kept inline here (rather than in a further nested helper) so that the
queue updates from each processed event are retained in this frame. On an error mid-
drain, recovery state lives in shared environments -- the objects and
\code{sim@.xData[["._rmo"]]} in \code{sim@.xData}, and \code{sim@completed} -- so it is preserved for
\code{\link[=restartSimInit]{restartSimInit()}} even though this function's local \code{sim} is discarded.
}
\keyword{internal}
