% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation-parseModule.R
\name{.fnsReachableFrom}
\alias{.fnsReachableFrom}
\title{Transitive closure of module-local functions reachable from a starting function}
\usage{
.fnsReachableFrom(startName, env)
}
\arguments{
\item{startName}{Character. Name(s) of the function(s) to start the walk from.}

\item{env}{The module's namespace environment (\code{sim@.xData$.mods[[module]]}).}
}
\value{
Character vector of function names (always including any of \code{startName}
that exist in \code{env}); cycle-safe.
}
\description{
Walks the call graph of \code{startName} within a single module's namespace
environment, following references to other functions defined in that same
environment (helpers in the main module file or in \verb{R/} sub-files), to any
depth. Used to scope the \code{.inputObjects} cache digest: the cacheId must change
when any function that \code{.inputObjects} actually calls is edited, but not when
unrelated module functions (e.g., \code{doEvent}, \code{Init}) change. Digesting only
\code{.inputObjects} lets a helper fix be silently masked by a stale cache entry
(most visibly on \code{\link[=restartSimInit]{restartSimInit()}}, whose rewind reproduces the exact state the
stale entry was keyed under); digesting the whole module env over-invalidates.
}
\details{
Only module-local functions are followed; package functions are out of scope
(handled via \code{reqdPkgs}, which is already part of the digested metadata, not by
walking package bodies). String-based dynamic dispatch (\code{do.call("helperA", )},
\code{get("helperA")()}) is invisible to \code{\link[codetools:findGlobals]{codetools::findGlobals()}}, so as a
supplement any string literal in a reached function that matches a module
function name is also followed. This is precise (a benign \code{do.call(rbind, )}
with a function \emph{symbol} is already caught by \code{findGlobals}, and triggers no
extra invalidation) rather than a blanket whole-module fallback.
}
\keyword{internal}
